import { add, multiply, PI } from './mathUtils.mjs';

const sum = add(10, 5);
const product = multiply(10, 5);

console.log(`Sum is: ${sum}`);       
console.log(`The product is: ${product}`); 
console.log(`Value of PI: ${PI}`);