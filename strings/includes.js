// --- String Example ---
const sentence = 'Mahilig ako sa kape';
const word = 'kape';

console.log(sentence.includes(word)); // Output: true

// --- Array Example ---
const basket = ['apple', 'banana', 'orange'];

if (basket.includes('apple')) {
    console.log('May mansanas sa basket!');
} else {
    console.log('Wala rito.');
}